# 更新记录

## 3.2.13-beta

- 把树莓派 4、树莓派 5 拆成独立设备配置：分别提供 416×416 与 640×640 起步建议，统一优先导出 ARM CPU 友好的 NCNN，并保留 ONNX 回退。
- 新增 RDK X5 Bayes-e NPU 全流程转换包：携带原始 `.pt`、中间 ONNX、代表性校准图片、官方 `rdk_model_zoo` 转换入口、转换计划、类别文件和 `hrt_model_exec` 校验脚本。
- 修正 RDK X5 最终产物定义为 INT8 `.bin`；部署清单区分中间 ONNX、待转换状态和最终产物，不再把 ONNX 或 `.hbm` 误报为 X5 NPU 模型。
- 部署页新增连续工作流、最终产物/运行时提示、“将设备建议带到训练页”和“一键使用训练图片作为校准来源”，切换设备时自动清理不兼容芯片参数。
- 保留旧 `raspberry_pi` 与 `drobotics_rdk` 配置 ID 的命令行和已保存设置兼容，同时新界面优先展示明确板型。
- 完成 71 项自动化、真实 YOLO11n → ONNX 导出与 ONNX Runtime 加载、真实浏览器设备切换、PowerShell 5.1 解析和 WebView2 桌面编译；RDK X5 最终 `.bin` 仍需在 OpenExplorer 与真机完成硬件验证。

## 3.2.12-beta

- 训练页新增始终可见的“训练前就绪检查”，进入页面后自动检查数据、模型、Python 环境与训练设备；修改关键参数后会自动重新检查并直接列出阻塞项。
- 工作台系统诊断新增“一键复制诊断报告”和“查看运行日志”，诊断报告包含 Python、GPU/CUDA、组件版本和具体错误，方便队友直接提交可复现的问题信息。
- 协作标注“项目与团队”改为渐进式管理面板：当前项目与进度固定显示，项目图片、流程、团队任务、导出和危险操作按需展开。
- 标注项目删除改为统一的页内安全弹窗，明确删除边界；只有输入完整项目名称后才能执行，不再连续弹出系统确认框和输入框。
- 完成 64 项自动化、Python 编译、JavaScript 语法和真实浏览器训练预检、诊断复制、管理折叠与安全删除回归。

## 3.2.11-beta

- 修复三份 Windows 安装/构建 PowerShell 入口脚本缺少 UTF-8 BOM 导致 Windows PowerShell 5.1 中文乱码、意外标记和字符串未终止的问题，并增加不可回退的编码测试。
- Windows 安装包同时写入规范的文件版本与产品版本，便于系统属性、升级检测和问题报告准确识别构建。
- 工作台总览新增按需系统诊断，集中显示 Python、PyTorch/CUDA、Ultralytics、OpenCV、ONNX Runtime 和导出组件状态。
- 系统诊断从“能否导入”提升为安装包与版本约束校验；可识别误装普通 OpenCV、版本越界和 ONNX 组件缺失，增量安装也会复用同一健康检查。
- 训练面板与协作标注服务改为独占端口监听；重复启动第二个实例会立即报端口占用并退出，不再出现两个进程随机处理请求。
- 协作标注中心新增管理员安全删除项目：只删除标注工作区托管副本和数据库记录，保留训练平台外部数据集和已导出压缩包，并要求项目名称确认。
- 完成 61 项自动化、Windows PowerShell 5.1 解析、JavaScript 语法、桌面编译和真实浏览器主导航/项目/诊断/协作同步回归。

## 3.2.10-beta

- 项目中心新增安全删除：默认只移除项目记录并保留数据、训练模型和导出产物；可选清理平台托管数据，必须输入项目名称确认。
- 删除范围严格限制在平台为该项目创建的专用数据目录，外部关联数据集、训练记录、模型资产和部署产物不会被连带删除。
- 删除当前项目后自动切换到剩余项目；没有剩余项目时仅清理项目关联设置，保留设备、模型和其他全局偏好。
- 新增项目编辑，可修改名称、任务类型、类别和备注；编辑未激活项目不会错误覆盖当前训练上下文。
- 新增“复制配置”，复制任务类型和类别到一个新的空项目，并立即激活，方便开展新一轮数据迭代。
- 新增项目搜索与数据状态筛选、数据集路径复制；图片预览默认折叠，项目较多时保持页面紧凑。
- 增加项目生命周期、托管目录删除边界和项目中心交互的自动化与真实浏览器测试。

## 3.2.9-beta

- 新增工程师导向的“工作台总览”，把项目、数据、训练、验证和部署串成五阶段流程，并根据实际状态推荐下一步。
- 左侧导航按“工作流 / 结果 / 资源”重新分组；自动跟踪标注默认隐藏到实验功能，减少主路径干扰。
- 新增 `Ctrl+K` 快速导航，可按页面、操作、设备和常用术语检索功能。
- 重构训练配置的渐进式信息架构：基础模型、设备、尺寸、轮数和批量保持可见，环境、远程训练与底层参数进入更多设置。
- 训练按钮固定在当前窗口底部；配置自动保存，训练运行时只显示“停止并保留最佳模型”。
- 数据导入默认只要求选择根目录，自动识别后的底层路径和旧格式转换选项进入高级设置。
- 模型测试按摄像头、单图和文件夹来源动态显示所需字段；INT8 校准路径仅在启用量化时出现。
- 外部模型登记与补充扫描折叠为按需工具，模型资产浏览和搜索回到页面主视线。
- 更新协作标注文案，明确公共队列自动领取、负责人分配和双人审核均为可选能力。
- 增加工作台流程、渐进式显示、核心训练参数和第 11 枚导航图标的自动化测试。

## 3.2.8-beta

- 项目中心的目标检测项目自动同步到个人/团队标注中心，按平台项目 ID 更新名称、类别和图片，重复同步不再产生重复项目或图片。
- 重做标注主流程为“选择项目与图片 → 选择类别并拖框 → 完成并下一张”，选择项目后自动打开首张图片。
- 个人和团队项目默认关闭审核；“完成并下一张”直接进入可导出数据集，双人审核改为管理员按项目主动开启的可选功能。
- 团队成员可直接查看项目并从公共队列自动领取图片，任务分配降级为可选的负责人控制。
- 标注画布新增贯穿图片的横竖虚线准星和实时 X/Y 坐标，移动、缩放和绘框时持续显示参考位置。
- 图片队列移动到类别和检测框之前，未保存状态明确提示；切换图片会自动保存进度，关闭页面时提醒未保存修改。
- 快捷键向常见标注工具靠拢：`B` 画框、`V/D` 选择、左右方向键切图、`Ctrl+Enter` 完成并下一张。
- 增加旧标注数据库无损迁移、项目同步幂等、默认免审核和画布准星的自动化测试。

## 3.2.7-beta

- 为项目、训练、数据集、基础模型、模型资产、测试、协作标注、快速标注、部署导出和运行记录绘制 10 枚语义化线性 SVG 图标。
- 侧栏收起后显示功能图标，不再使用 01–10 编号；每个入口保留原生悬停提示和可访问名称。
- 展开侧栏统一采用“图标 + 名称”布局，当前页面图标使用品牌色高亮，移动端同步保留图标与文字。
- 增加导航图标完整性、图标引用和收起状态显示规则的自动化测试。

## 3.2.6-beta

- 桌面端升级为全视口工作台，取消 1440px 内容宽度上限，充分利用宽屏和 WebView2 窗口。
- 顶部品牌区压缩为 64px 紧凑状态栏，减少装饰性留白，让项目、训练和标注内容更早进入视野。
- 左侧导航改为贴边固定工作区，并支持一键收起到 68px；收起状态会保存在本机，下次打开自动恢复。
- 主内容区独立滚动，导航和平台状态始终可见；窄窗口仍自动切换为顶部横向导航和自然页面滚动。
- 增加桌面全视口、侧栏状态持久化和移动端回退布局的自动化测试。

## 3.2.5-beta

- 新增独立“官方基础模型中心”，可按系列、任务和 n/s/m/l/x 档位浏览可用权重及本机下载状态。
- 首批支持 YOLO26、YOLO11 和 YOLOv8 的目标检测与图像分类官方预训练模型。
- 模型通过已安装的 Ultralytics 从官方 assets 下载，下载过程进入统一运行日志；临时文件完整校验后才替换正式模型。
- 下载完成后自动保存为当前训练基础模型，也可从模型中心随时切换已经下载的权重。
- 增加资源等级、边缘设备提示、官方文档入口和 Ultralytics AGPL-3.0 / Enterprise License 提醒。
- 下载接口仅接受内置白名单中的官方文件名，拒绝任意 URL 和路径，避免把模型中心变成不受控下载器。

## 3.2.4-beta

- Windows 新版安装包默认采用增量更新，复用已有 Python、PyTorch、ONNX Runtime 和满足要求的平台依赖。
- 已安装 .NET 8 Desktop Runtime 或 WebView2 Runtime 时跳过对应安装程序，不再重复执行系统运行库安装。
- 增加依赖状态指纹与本地健康检查；依赖清单未变化且环境健康时完全跳过 pip 下载。
- 依赖发生变化时取消全量 `--upgrade`，只补装缺少或不兼容的组件。
- 已安装用户可在安装向导中主动选择“完整修复运行环境”，安全删除并重建当前安装目录内的 Runtime；Workspace 始终保留。
- 安装日志区分首次安装、增量更新和完整修复，不再把每次升级都显示成首次部署。

## 3.2.3-beta

- 新建检测和分类数据集默认采用训练 80% / 验证 10% / 测试 10% 三段式划分。
- 训练页增加独立的训练集、验证集比例控制和自动计算的测试集占比；训练与验证之和为 100% 时可兼容关闭测试集。
- 检测数据自动生成 `images/test`、`labels/test` 并把 `test:` 写入 `dataset.yaml`；分类数据在每个类别内分别划分三个子集。
- 训练结束后自动使用 `best.pt` 对测试集执行独立评估，本机与远程 Windows 训练都会归档 `test-evaluation` 结果。
- 模型资产页标记已经完成独立测试评估的训练记录，训练清单同步记录数据划分比例和测试评估目录。
- 协作标注导出的 YOLO/VOC 数据集统一调整为稳定的 80% / 10% / 10% 划分。

## 3.2.2-beta

- 重做训练平台主题：采用冷灰工作台背景、清晰白色内容面和克制的品牌红，降低长时间使用时的视觉疲劳。
- 重新整理导航、页面标题、数据概览、表单、按钮、状态和日志的视觉层级，训练、导出与危险操作使用独立语义色。
- 优化窄窗口和移动端布局：侧栏自动切换为顶部横向导航，并自动定位当前功能页。
- 页面切换后自动回到内容顶部，同步更新窗口标题和 `aria-current`，改善可发现性与键盘/辅助技术体验。
- 增加减少动态效果偏好支持，并统一焦点、悬停、禁用、只读和滚动条样式。

## 3.2.1-beta

- 修复已有源码版占用 8989 端口时，安装版桌面程序只显示“本地服务启动失败”的问题。
- 新增桌面程序单实例与训练面板单服务策略；发现旧服务时询问是否关闭，拒绝后保留旧服务并退出当前窗口。
- 用户确认后只关闭已验证的 YOLO 训练面板进程；普通软件占用默认端口时才选择安全的本机备用端口。
- 新增 `Workspace/logs/launcher.log`，完整记录服务管理器输出、端口和异常信息。
- beta 标签允许生成未签名测试安装包；不带预发布后缀的稳定版仍强制要求可信代码签名。

## 3.2.0-beta

- 新增项目中心：统一关联任务、类别、数据集、训练配置和模型资产。
- 新增数据集健康检查与图片预览，识别缺标注、孤立标注、坏图和无效 YOLO 坐标。
- 模型资产支持手动登记已有 `.pt` / `.onnx`，并可勾选多个模型对比。
- 协作标注支持浏览器上传伙伴本机图片、移动/缩放/改类、撤销/重做、缩放和快捷键。
- 主界面改为项目优先的紧凑布局，并补充表单可访问性关联。
- 增加保留权利 `LICENSE`、第三方声明和 Windows EXE/Setup 强制签名发布配置。

## 3.0.2-beta

- 修复卸载后残留 Python、PyTorch、ONNX Runtime 和 Python 缓存的问题。
- 卸载时始终清理当前实际安装目录下的 `App`、`Desktop` 和 `Runtime`，不使用安装根目录通配删除。
- 交互卸载增加 Workspace 用户数据保留选项；静默卸载默认保留数据，也可使用 `/PURGEDATA` 明确彻底删除。
- 仅在安装目录为空时删除安装根目录，未知文件和安装目录之外的内容不会被处理。

## 3.0.1-beta

- 修复 Windows PowerShell 5.1 将 UTF-8 中文安装脚本错误解码后导致运行环境安装中断的问题。
- 构建安装包时强制将运行环境入口写为 UTF-8 BOM，并使用 Windows PowerShell 5.1 做发布前语法检查。
- 安装 Python、PyTorch、ONNX Runtime 等运行依赖失败时，不再错误启动尚未就绪的桌面程序。
- 安装器新增实时滚动日志页面，可直接查看 .NET、WebView2、Python、PyTorch 和 ONNX Runtime 安装进度。
- 运行环境探测不再把“尚未安装 PyTorch”的正常状态记录成红色异常。

## 3.0.0-beta

- 平台统一更名为“YOLO团队训练平台”。
- 增加 WebView2 Windows 桌面封装和 Inno Setup 一键安装器，默认安装到 `D:\YOLOTeamTrainingPlatform`。
- Windows 自动准备 .NET、WebView2、Python 3.10–3.14、PyTorch、ONNX Runtime 和全部平台依赖。
- 建立统一 `Workspace` 布局与不可覆盖的训练、导出、测试和项目包命名规范。
- 模型资产仅接受带 `training-manifest.json` 的可追溯训练记录，不再推断或迁移旧训练目录。
- 协作标注项目包升级为 `.ytp-project.zip`，并统一数据集导出清单。
- Ubuntu 安装与运行路径同步采用新版 Workspace 规范。

## 2.3.0-beta

- 增加不依赖 Docker 的本地优先协作标注中心。
- 同一套程序支持个人模式和局域网共享模式，任何电脑都可以临时成为团队主机。
- 增加管理员、审核员、标注员账号和任务分配。
- 增加五分钟编辑锁、修订冲突检查、提交、审核通过和驳回流程。
- 增加可移植 `.matproj.zip` 项目包，支持独立工作区之间交换图片和标注。
- 增加 Ultralytics YOLO、COCO、Pascal VOC 和 LabelMe 导出，只导出审核通过的数据。
- 增加 Windows 与 Ubuntu 的个人/共享标注启动和停止工具。

## 2.2.0-beta

- 增加独立“模型资产”页面，按数据集归组展示训练记录、PT/ONNX 和设备部署产物。
- 新训练自动生成 `training_manifest.json`，记录数据集、类别、参数、指标和模型路径。
- 支持扫描旧版 `outputs_*` 目录，并明确区分“清单关联”和“推断关联”。
- 模型资产可一键回填到测试页面或部署页面。
- 增加本机 `model_registry.json` 索引，记录已扫描目录和训练/部署清单。

## 2.1.0-beta

- 增加通用 ONNX、树莓派 NCNN、Rockchip RKNN、地瓜 RDK、MaixCAM、TensorRT 和 OpenVINO 设备配置档。
- 增加独立的多平台导出工具、部署清单和 ONNX Runtime 加载检查。
- 将 MaixCAM 专用转换降为兼容旧流程，不再作为项目唯一部署目标。
- 半自动标注增加漂移质量判断；任一目标异常时整帧停止保存，避免生成缺框标签。
- 网页跟踪器改为面向用户的策略名称，并增加待复核计数和异常原因。
- 增加设备适配、半自动标注、路线图和第三方来源核对文档。
- 分享包改名为 `MyAutoTrain-日期时间.zip`，并包含 `docs` 文档目录。
- 默认 SSH 端口修正为 22，面板直接运行时默认仅监听 `127.0.0.1`。

## 2.0.0-team

- 增加队友电脑一键安装与启动流程。
- 自动识别 NVIDIA CUDA 或 CPU 训练环境。
- 增加安装后系统自检和 JSON 检查报告。
- 增加一键制作团队分享 ZIP 包。
- 主界面改为红白浅色主题。
- 默认页面只保留数据选择、训练预设和开始/停止操作。
- 低频参数和设备转换收进“更多设置”。
- 默认使用 640×480、batch 16、4 个加载进程和手动停止模式。
- 增加 Ubuntu 部署、启动和停止脚本，支持 NVIDIA CUDA 12.8 或 CPU 环境。
- 安装依赖补充 `onnxruntime`，系统自检会报告其缺失状态。
- Python 支持范围扩展为 3.10–3.14。
