# 安全说明

YOLO团队训练平台会执行训练、导出和文件选择操作，默认仅监听 `127.0.0.1:8989`，没有远程账号系统。协作标注中心是独立服务：个人模式监听 `127.0.0.1:9000`；共享模式监听局域网接口并提供管理员、审核员和标注员账号。

- 不要把 8989 端口直接暴露到公网。
- 不要把协作标注的 9000 端口通过路由器映射到公网；共享模式只应用于可信局域网。
- 首位管理员应使用强密码；不要在多个公开项目中复用密码。
- 远程使用请通过 SSH 端口转发或带身份验证的反向代理。
- 不要导入来源不明的模型、数据脚本、压缩包或转换工具链。
- 发布日志前移除用户名、路径、IP、SSH 信息和数据集内容。
- 标注工作区包含账号数据库、原始图片和审核记录，不应上传 GitHub 或放入公开部署包。

报告安全问题时，请避免在公开 Issue 中披露可直接利用的细节。先通过仓库维护者公开资料中的私密联系方式联系维护者；维护者应在 GitHub 仓库设置中补充专用安全联系渠道。
